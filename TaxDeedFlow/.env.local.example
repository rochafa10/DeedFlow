# ===========================================
# Tax Deed Flow - Environment Configuration
# ===========================================

# Copy this file to .env.local and fill in your actual values
# cp .env.local.example .env.local

# ===========================================
# Supabase Configuration (REQUIRED)
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# Logger Configuration (OPTIONAL)
# ===========================================
# Override the default log level for your environment
# Defaults:
#   - development: DEBUG (all logs)
#   - production: WARN (warnings and errors only)
#   - test: ERROR (errors only)

# NEXT_PUBLIC_LOG_LEVEL=DEBUG   # Show all logs (development)
# NEXT_PUBLIC_LOG_LEVEL=INFO    # Show info, warnings, and errors
# NEXT_PUBLIC_LOG_LEVEL=WARN    # Show only warnings and errors (production default)
# NEXT_PUBLIC_LOG_LEVEL=ERROR   # Show only errors (test default)
# NEXT_PUBLIC_LOG_LEVEL=NONE    # Disable all logging

# ===========================================
# External API Keys (OPTIONAL)
# ===========================================
# Required for full functionality, but app works without them

# Regrid API for property data enrichment
# REGRID_API_KEY=your-regrid-api-key

# Mapbox for mapping features
# NEXT_PUBLIC_MAPBOX_TOKEN=your-mapbox-token

# Other integrations as needed
